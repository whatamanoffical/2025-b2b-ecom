[build]
builder = "nixpacks"
buildCommand = "echo \"DATABASE_URL=$DATABASE_URL\" > .env && yarn install --immutable && yarn medusa db:create && yarn medusa db:migrate && yarn run seed && yarn medusa user -e admin@test.com -p supersecret -i admin && yarn build"

[deploy]
startCommand = "yarn start"
healthcheck = "/health"
